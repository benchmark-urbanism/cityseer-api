{
  "scripts": {
    "generate": "node generateDocs.js",
    "lint": "markdownlint 'docs/**/*.md' --fix",
    "dev": "yarn run generate && yarn run lint && vitepress dev docs",
    "build": "yarn run generate && yarn run lint && vitepress build docs",
    "serve": "yarn run generate && yarn run lint && vitepress serve docs"
  },
  "dependencies": {},
  "devDependencies": {
    "@iktakahiro/markdown-it-katex": "^4.0.1",
    "@prettier/plugin-pug": "^1.13.4",
    "autoprefixer": "^10.2.4",
    "eslint": "^7.20.0",
    "eslint-config-prettier": "^8.0.0",
    "eslint-plugin-prettier": "^3.3.1",
    "eslint-plugin-vue": "^7.6.0",
    "markdown-it-replacements": "^1.0.1",
    "markdownlint-cli": "^0.26.0",
    "postcss": "^8.2.6",
    "prettier": "^2.2.1",
    "pug": "^3.0.0",
    "pug-plain-loader": "^1.1.0",
    "python-shell": "^2.0.3",
    "stylelint": "^13.11.0",
    "stylelint-config-prettier": "^8.0.2",
    "stylelint-config-standard": "^20.0.0",
    "tailwindcss": "^2.0.3",
    "vitepress": "^0.12.2"
  }
}
