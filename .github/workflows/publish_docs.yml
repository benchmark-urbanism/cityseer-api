name: publish docs
on:
  push:
    branches:
      - master
jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@main
      - name: build docs
        run: |
          pip install pydoc docspec-python
          yarn install
          yarn run build
      - name: deploy docs
        uses: alex-page/blazing-fast-gh-pages-deploy@v1.1.0
        with:
          repo-token: ${{ secrets.GH_PAT }}
          site-directory: docs/.vitepress/dist
