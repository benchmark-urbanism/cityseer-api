---
import { BookOpen } from '@vicons/fa'

import CommonLayout from '@src/layouts/CommonLayout.astro'
import Footer from '@src/components/Footer.astro'
import RepoLink from '@src/components/RepoLink.astro'
import StripeTitle from '@src/components/StripeTitle.astro'
import logoURL from '@src/assets/logos/cityseer_logo_light_red.png'

import '@src/styles/global.css'
---

<CommonLayout>
  <Fragment slot="metaContent">
    <title>Cityseer API Docs</title>
  </Fragment>
  <div id="home-container" class="w-full justify-center">
    <aside id="left-container">
      <div class="flex flex-auto flex-col items-center justify-center">
        <div class="grow"></div>
        <div class="max-w-xs object-contain">
          <img class="h-full w-full px-4 py-16" src={logoURL.src} alt="logo" />
        </div>
        <a id="go-button" href="/intro" title="Get Started!">
          <button
            id="go-button"
            class="border-theme bg-dark-grey text-theme flex h-16 w-16 items-center justify-center rounded-full border shadow-xs shadow-black"
          >
            <BookOpen class="w-8" />
          </button>
          <h2 class="py-4 leading-none">Documentation</h2>
        </a>
        <div class="flex flex-col items-center py-8 py-10">
          <RepoLink />
        </div>
      </div>
      <StripeTitle />
    </aside>
    <div id="right-container">
      <div class="grow"></div>
      <h1>Computational tools for urban analysis</h1>
      <h2>Morphological analysis</h2>
      <p>
        Developed from the ground-up for urban analysis: assess the precursors to vibrant
        neighbourhoods using street networks, mixed-uses, landuse accessibilities, and spatial
        statistics.
      </p>
      <h2>Pedestrian scale</h2>
      <p>
        High-resolution analysis at the pedestrian scale with metrics computed at intervals along
        streetfronts: use network distances and spatial impedances for enhanced contextual
        sensitivity.
      </p>
      <h2>Friendly & performant</h2>
      <p>
        Python package with heavier algorithms implemented in paralellised `rust` code. I/O
        convenience methods for NetworkX with advanced cleaning methods for OSM data. Generated
        metrics stored as GeoPandas for export to common geospatial formats.
      </p>
      <Footer />
    </div>
  </div>
</CommonLayout>

<style>
  #home-container {
    @apply flex items-center justify-center;
  }

  #left-container {
    @apply flex max-w-xl flex-1 px-6 py-8;
  }

  #right-container {
    @apply flex max-w-xl flex-1 flex-col px-6 py-8;
  }

  #go-button {
    @apply flex flex-col items-center justify-center transition-all;

    &:hover {
      @apply scale-105;
    }
  }

  @media only screen and (max-width: 958px) {
    #home-container {
      @apply w-full flex-col items-center;
    }

    #left-container {
      @apply w-full border-r-0;
    }

    #right-container {
      @apply max-w-none justify-start;
    }
  }
</style>
